{
  "name": "gatehouse",
  "version": "0.1.0",
  "description": "An agent-native front door for open-source bug reports: a repro must fail on the bad build and pass on the good one before the submit tool exists.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test $(find tests src evals -name \"*.test.mjs\" | sort)",
    "dev": "node scripts/dev-server.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}